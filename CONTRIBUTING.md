# Contributing

Contributions must be provider-free and use exclusively synthetic fixtures.
Before opening a pull request, run:

```sh
npm test
npm run check:public-safety
```

Do not add secrets, real project names, platform integrations, private evidence
or artifacts derived from a research run.
