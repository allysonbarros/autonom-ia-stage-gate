# Contracts

This package defines canonical JSON, synthetic-fixture validation and
tamper-evident receipts for the public provider-free workflow.

It accepts only deliberately synthetic, scientifically ineligible input. It does
not connect to the platform, accept a credential or dispatch a model.
