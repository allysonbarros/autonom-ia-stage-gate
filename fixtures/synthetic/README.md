# Synthetic fixtures

Every fixture in this directory must be deliberately invented, provider-free,
and marked `scientific_eligible: false` where that field applies.

Do not transform, redact, rename or partially copy a platform project, wizard,
capsule, artifact or evidence file into this directory.
